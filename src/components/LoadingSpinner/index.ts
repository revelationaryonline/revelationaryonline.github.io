import LoadingSpinner from './LoadingSpinner';

// Only export the LoadingSpinner component
// We're now importing LoadingWrapper directly in App.tsx to avoid circular imports
export default LoadingSpinner;
